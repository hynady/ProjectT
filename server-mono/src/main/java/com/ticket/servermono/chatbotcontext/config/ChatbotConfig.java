package com.ticket.servermono.chatbotcontext.config;

import org.springframework.context.annotation.Configuration;
import org.springframework.validation.annotation.Validated;

/**
 * Configuration class for chatbot context.
 * Ensures validation is properly configured.
 */
@Configuration
@Validated
public class ChatbotConfig {
    
    // This configuration class ensures that validation is enabled
    // for the chatbot context package
}
